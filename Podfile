# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'App-Test-jenkins' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for App-Test-jenkins

  target 'App-Test-jenkinsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'App-Test-jenkinsUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
